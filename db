DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'your_database_name',       # Replace with your DB name
        'USER': 'your_database_user',       # Replace with your DB username
        'PASSWORD': 'your_db_password',     # Replace with your DB password
        'HOST': 'localhost',                # Use 'localhost' or your server IP
        'PORT': '5432',                     # Default PostgreSQL port is 5432
    }
}



pip install python-dotenv

import os
from dotenv import load_dotenv

# Load variables from .env file into the system environment
load_dotenv()

# Access individual variables safely
db_host = os.getenv("DB_HOST")
api_secret = os.getenv("API_SECRET")

# Provide a fallback default value if the variable doesn't exist
port = os.getenv("PORT", "8080") 

print(f"Connecting to {db_host}")

